name: 📊 Profile Dashboard Update

on:
  workflow_dispatch: # 🔄 Allow manual trigger
  schedule:
    - cron: '0 0 * * 0'  # 🕛 Every Sunday at midnight UTC

jobs:
  update-profile:
    name: 🔄 Update GitHub Profile Dashboard
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_ACTOR: ${{ secrets.GH_ACTOR }}
      GH_EMAIL: ${{ secrets.GH_EMAIL }}
      GH_USERNAME: ${{ secrets.GH_USERNAME }}
    steps:
      - name: 🔍 Validate Required Secrets
        run: |
          missing_secrets=()
          
          # Check WakaTime related secrets
          if [ -z "${{ secrets.WAKATIME_API_KEY }}" ]; then
            missing_secrets+=("WAKATIME_API_KEY")
          fi
          
          # Check GitHub related secrets
          if [ -z "${{ secrets.GITHUB_TOKEN }}" ]; then
            missing_secrets+=("GITHUB_TOKEN")
          fi
          
          if [ -z "${{ secrets.GH_TOKEN }}" ]; then
            missing_secrets+=("GH_TOKEN")
          fi
          
          if [ -z "${{ secrets.GH_EMAIL }}" ]; then
            missing_secrets+=("GH_EMAIL")
          fi
          
          if [ -z "${{ secrets.GH_USERNAME }}" ]; then
            missing_secrets+=("GH_USERNAME")
          fi

          if [ -z "${{ secrets.GH_ACTOR }}" ]; then
            missing_secrets+=("GH_ACTOR")
          fi
          
          # Show error if any secrets are missing
          if [ ${#missing_secrets[@]} -ne 0 ]; then
            echo "❌ Error: The following required secrets are missing: ${missing_secrets[*]}"
            echo "Please define them in the repository settings."
            exit 1
          fi
          
          echo "✅ All required secrets are defined."

      - name: 📜 Setup Git Configuration
        run: |
          git config --global user.name "${{ secrets.GH_ACTOR }}"
          git config --global user.email "${{ secrets.GH_EMAIL }}"
          git config --global init.defaultBranch main
      
      - name: ⏱️ Get Start Time
        id: start-time
        run: echo "time=$(date +%s)" >> $GITHUB_OUTPUT

      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1 # Fetch only the latest commit to speed up the process
          persist-credentials: false # Do not use the default token for authentication
          ref: main # Specify the branch to checkout
          token: ${{ secrets.GH_TOKEN }} # Use the GitHub token for authentication
          clean: False # Do not clean the working directory for faster checkout
          lfs: false # Do not use Git LFS 
          submodules: false # Do not fetch submodules
          show-progress: true # Show progress during checkout
          path: ./ # Specify the path to checkout the repository

      # Setup cache for WakaTime
      - name: 🗄️ Setup Cache for WakaTime
        uses: actions/cache@v3
        id: cache
        continue-on-error: true
        if: always() # Always run this step
        timeout-minutes: 10
        with:
          path: .cache
          key: wakatime-cache-${{ github.repository }}-${{ github.run_number }}-${{ hashFiles('.github/workflows/update-profile.yml') }}
          restore-keys: |
            wakatime-cache-${{ github.repository }}-
            wakatime-cache-

      - name: 📋 Handle Cache Status
        run: |
          # Check if cache was hit or not
          if [ -d ".cache" ]; then
            echo "Cache restored successfully."
          else
            echo "No cache found, creating a new one."
            mkdir -p .cache
          fi
          # Save the current stats to the cache
          if [ -f "stats.json" ]; then
            cp stats.json .cache/last_stats.json
          fi
          echo "Cache hit: ${{ steps.cache.outputs.cache-hit }}"

      - name: 📊 Update README with WakaTime Stats
        uses: athul/waka-readme@master
        timeout-minutes: 10
        continue-on-error: true
        id: wakatime-stats-update-readme
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPOSITORY: ${{ secrets.GH_USERNAME }}/${{ secrets.GH_USERNAME }}
          SHOW_TITLE: true
          AUTHOR_EMAIL: ${{ secrets.GH_EMAIL }}
          AUTHOR_NAME: ${{ secrets.GH_ACTOR }}
          COMMIT_MESSAGE: "chore(stats): update README.md with WakaTime Readme [skip ci]"
          COMMITTER_EMAIL: ${{ secrets.GH_EMAIL }}
          COMMITTER_NAME: ${{ secrets.GH_ACTOR }}
          TARGET_BRANCH: main
          TARGET_PATH: README.md
          SHOW_MASKED_TIME: true
          SHOW_TIME: true
          SHOW_TOTAL: true
          SECTION_NAME: "waka-readme"

      - name: 🔍 Check for significant changes
        id: check-changes
        run: |
          # Compara estadísticas actuales con las anteriores
          if [ -f ".cache/last_stats.json" ]; then
            # Lógica para comparar y decidir si actualizar
            echo "::set-output name=should_update::$(python3 -c 'import sys, json, os; print("true" if not os.path.exists(".cache/last_stats.json") or json.load(open(".cache/last_stats.json"))["total_coding_time"] != json.load(sys.stdin)["total_coding_time"] else "false")')"
          else
            echo "::set-output name=should_update::true"
          fi

      - name: 📊 Update WakaTime Stats
        if: steps.check-changes.outputs.should_update == 'true'
        uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          SECTION_NAME: "waka"
          SHOW_LINES_OF_CODE: "False"
          SHOW_PROFILE_VIEWS: "False"
          SHOW_COMMIT: "False"
          SHOW_DAYS_OF_WEEK: "True"
          SHOW_LANGUAGE: "True"
          SHOW_OS: "True"
          SHOW_PROJECTS: "False" 
          SHOW_EDITORS: "True"
          SHOW_LANGUAGE_PER_REPO: "True"
          SHOW_SHORT_INFO: "True"
          SHOW_LOC_CHART: "False"
          SHOW_UPDATED_DATE: "False"
          SHOW_TOTAL_CODE_TIME: "False"
          SHOW_TIMEZONE: "False"
          COMMIT_MESSAGE: "chore(stats): update wakatime stats [skip ci]"
          IGNORED_REPOS: "[`${{ secrets.GH_USERNAME }}/${{ secrets.GH_USERNAME }}']" # Ignore own repository
          COMMIT_SINGLE: "False"
          DEBUG_LOGGING: "True"
          PULL_BRANCH_NAME: "main"
          PUSH_BRANCH_NAME: "main"
          COMMIT_EMAIL: "${{ secrets.GH_EMAIL }}"
          COMMIT_USERNAME: "${{ secrets.GH_ACTOR }}"

      - name: 📈 Registrar Métricas
        run: |
          START_TIME="${{ steps.start-time.outputs.time }}"
          END_TIME="$(date +%s)"
          DURATION=$((END_TIME - START_TIME))
          echo "Duración total: $DURATION segundos" >> $GITHUB_STEP_SUMMARY
          echo "Caché hit: ${{ steps.cache.outputs.cache-hit }}" >> $GITHUB_STEP_SUMMARY

      - name: 📊 Mostrar Resumen
        run: |
          echo "### Resumen del Workflow" >> $GITHUB_STEP_SUMMARY
          echo "- **Duración total:** $DURATION segundos" >> $GITHUB_STEP_SUMMARY
          echo "- **Caché hit:** ${{ steps.cache.outputs.cache-hit }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Estado del Workflow:** ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Fecha y hora de ejecución:** $(date)" >> $GITHUB_STEP_SUMMARY
          echo "- **Último commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Autor:** ${{ secrets.GH_ACTOR }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Email:** ${{ secrets.GH_EMAIL }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Repositorio:** ${{ github.repository }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Acción:** ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **URL del Commit:** ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
      
      - name: 📧 Notify Result
        run: |
          STATUS="${{ job.status }}"
          echo "::notice ::Workflow completado con estado: $STATUS"
          
          # Puedes expandir esta sección para enviar notificaciones 
          # por correo o a servicios como Slack, Discord, etc.
          
          if [ "$STATUS" = "success" ]; then
            echo "✅ Profile updated successfully!"
          elif [ "$STATUS" = "failure" ]; then
            echo "❌ Profile update failed!"
          else
            echo "⚠️ Profile update status: $STATUS"
          fi